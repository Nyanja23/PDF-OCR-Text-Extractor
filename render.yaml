services:
  - type: web
    name: pdf-ocr-extractor
    env: docker
    dockerfilePath: docker/Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false
